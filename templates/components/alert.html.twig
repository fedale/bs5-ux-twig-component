<div class="alert alert-{{ type }} {{ dismiss ? 'alert-dismissible fade show' : '' }}" role="alert" id="{{ id }}" data-controller="alert">
{{ content }}

{% if dismiss %}
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
{% endif %}
</div>