twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        tabler:
            dir: ltr # see if it is present in symfony/easyadmin
            theme: light
            layout: fluid # blank boxed fluid 

when@test:
    twig:
        strict_variables: true
